# CarrierPress - Developed by M6VPN (M6VPN@tuta.com)
# carrierpress/configs/default.conf

# Ordinary workflow defaults for local testing.
# Audio-chain processing belongs in profiles, not config files.
profile = profiles/am-safe.profile
audio_backend = auto
sample_rate = 48000
channels = 2
block_size = 256
meter_interval_ms = 1000
tui = off
gui = off
